public int eliminarDato(Pila pila, int n)
{
	int numELemsElim = 0;
	
	if(!(pila.Pilavacia())
	{
		int elemento = pila.desapilar();
		numELemsElim = pila.eliminarDato(pila, dato);
		
		if(elemento != dato)
		{
			pila.apilar(elemento));
		}
		else
		{
			numELemsElim++;
		}
		
			
	return numELemsElim;
	}
}
		